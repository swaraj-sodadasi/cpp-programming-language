#include <iostream>  // Provides standard input and output stream objects
using namespace std;

int main() {
    // Prints message to the standard console output stream followed by a newline
    cout << "Hello World. This is a demo program to check whether build and execution is perfect\n"; 
    
    // Explicitly returns zero to indicate successful program execution to the OS
    return 0; 
}